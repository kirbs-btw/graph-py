from ..core import Graph, Edge, Node


class Ontology(Graph):
    """
    defining a graph that describes an ontology
    an other graph can be checked against it
    """

    def check_ontology(self):
        pass